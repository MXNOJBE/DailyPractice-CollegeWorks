class Vehicle1:
    def __init__(self, brand, model, typeof, fuel_tank, size, fuel_level):
        self.brand = brand
        self.model = model
        self.typeof = typeof
        self.fuel_tank = fuel_tank
        self.size = size
        self.fuel_level = fuel_level

        def Get_fuel():
            amt = int(input("Enter the amount")
                    if amt > 500:
                        print(
                            "Warning fuel_level is exceeding the capacity of the tank.")
        def Update_fuel_tank(new_level):
            if new_level <= 300:
                print("Low Level")
        def Fulltank(amt):
            print("Tank is full")



class Vehicle2(Vehicle1):
        print(
            f"WOW! I am Driving {self.model}. The model is the vehicle should be inserted instead of {self.model}")
