def birthDate(dic{"name"):}:
    dic = {}
    n = int(input("Enter the number of entries you want:: "))

    for x in range(n):
        name = input("Enter the Player's Name: ")
        dob = input("Enter the Date of Birth: ")

    dic[name] = dob

birthDate()
